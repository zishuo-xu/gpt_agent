# MyAgent — 验收文档

> 验收以"用户怎么使用"为主线组织测试用例。每条用例包含：前置条件、操作步骤、通过标准。

## 验收状态总览

> 更新时间：2026-08-03（一期收官后持续迭代，仓库 github.com/zishuo-xu/gpt_agent，CI 已接入）

- **已通过真实验证（29 项，全量）**：A1 A2 A4 B1 B2 B3 B4 C1 C2 C3 C4 C5 C6 D1 D2 D3（页面内）E1 E2 E3 E5 F1 F2 G1 G2 H1 H2 H3 I1 I2
  - D2 补充：内置模型价格表（Claude/DeepSeek/Kimi/GLM/Qwen），未显式配价模型按名自动匹配；
    `--budget` 开箱即用，实测每轮金额显示（≈¥0.0036）且预算熔断逻辑有单测覆盖
- **一期后新增能力**（非验收用例，已推送）：外部 webhook 推送（企业微信/飞书/Bark/通用）、会话删除、设置页 schema 驱动（renderer + ObjectFieldEditor 通用回退）、多项目切换（?project= + 监控台切换器）、内置模型价格表、GitHub Actions CI（typecheck+build+test）、**会话页对话链路面板**（按轮次列出提问、点击定位）、**会话树分支**（/tree 回溯 + /branch 分裂 + Web 分支树侧栏 + /goto 切换）、**/model 会话内热切换 main 模型**
- **一期真实验收中发现并修复的缺陷**：
  - 历史会话发消息 400 — `/input` 端点只读 `task` 字段，前端发 `message`（已兼容两者）
  - `server.host` / `server.password` 配置写入不生效 — 标量循环写顶层扁平键、读取读嵌套（已统一为嵌套路径）
  - `/init` 过度探索 — 模型反复跑测试命令不落盘，481k tokens 无产出（已强化 prompt 约束 + AgentLoop 新增轮数上限）
  - `/run --bounds "不改任何文件"` 未生成全量写保护 — fail-unsafe（已支持中文全量语义 → `Edit(**)` 等 deny）
- **二期迭代（生产级实测发现并修复）**：
  - **工具参数键名自强化循环**：历史回传用 camelCase（`filePath`）导致模型模仿错误键名、空参数错误频发。已修复：输入容错双键名（`file_path`/`filePath`）+ 历史回传还原 wire 格式（snake_case）+ 报错文案用 wire 键名。实测 49 次工具调用 0 参数错误
  - **配置变更不生效**：`applyConfigChange` 只更新权限档，改 API Key 后运行中会话仍用旧 Key。已修复：配置变更时用最新配置重建 main/cheap/explore 模型客户端并替换（含压缩、子代理），会话历史不丢失
  - **会话标题截断**：`slice(0,20)` 硬切产生英文残词。已修复：`clipTitle` 词边界截断 + 中文语言守卫 + 失败兜底
  - **缓存命中率低（4%）**：todo 在 system 中高频变化破坏整段前缀缓存。已修复（缓存前缀工程）：todo 移出 system 作为独立消息注入、跨项目索引会话内缓存一次、**记忆类静态段会话内只构建一次**（参照 Pi 的 AGENTS.md 启动时加载，写记忆不刷新当前会话）。实测三阶段：第 2 轮 4% → 66.4% → 静态段固定后 system 唯一值 1、第 1 轮命中 97.2%（跨会话缓存）、第 2 轮 65.6%
  - **项目级配置 Key 覆盖全局**：`local.jsonc` 中非空 Key 会覆盖全局新 Key，导致"测试连接成功但会话报余额不足"。已定位并更新项目配置；**设置页已加覆盖提示**（已落地）：`GET /api/config/key-overrides` 返回当前项目 local.jsonc 中非空 Key 的供应商，保存全局 Key 时若有覆盖则黄色警告并提示清空项目 Key；API Key 输入框旁同时显示内联提示
  - **工具输出截断差异化**（参照 Pi"控制单轮注入量"）：Bash/Read/Grep/Glob 各自独立上限（Bash 150 行/12KB、Read 256 行/30KB、Grep 200 行/20KB、Glob 300 行/8KB），进一步压缩每轮新增量
  - **生产级场景验证**：在 kanban-web 项目完成三轮增量开发（建站 → 优先级功能 → 自审查），60 个测试全过、lint 0 警告、浏览器实操验证（搜索过滤/优先级徽章/持久化）

### 三期真实验收（B/C/G/H 剩余 10 项全部补齐，2026-08-03）

- **B1 软遗忘**：实测 10 轮读文件，第 1→2 轮输入 +2044 tokens，第 9→10 轮 +188（增长率降 10 倍）；占位符化只压缩 tool 消息、assistant 摘要保留，模型可从摘要直接恢复关键信息（goldenRatioHelper 函数名），不必重新 Read
- **B2 硬压缩**：低阈值（临时 2000）下自动压缩触发（比例 0.622，context_compacted 事件）；压缩后仍记得前文 pnpm 约定
- **B4 打断**：软打断 user_queued 排队、本轮结束后自动处理；interrupt 立即中止模型与当前工具
- **C3 黑名单豁免不存在**：trust 档 `rm -rf` 直接拒绝（reason：命中 deny 规则，不能临时强制放行），无放行入口；工具错误回灌后 Agent 换方案
- **C4 无人值守 fail-closed**：审批 15s 超时自动拒绝（notify + permission_denied），Agent 拒绝后继续行动不僵死；再次需审批而无人响应时停留在 waiting_permission 属正确兜底
- **C6 拒绝并留言**：拒绝理由回灌（“别用 npm，用 pnpm”）后 Agent 改用 `pnpm add -D lodash`
- **G1 上下文隔离**：子代理 550k tokens 探索，主会话仅收到结论摘要（2.5k 字符）；缺陷修复见下
- **G2 子代理失败降级**：子代理内部 permission_denied 后优雅收尾，主 Agent 收到结论后自行验证收尾，主循环不中断
- **H2 跨项目联想**：项目 B（/tmp/myagent-h2-projB）的 Chroma 踩坑被项目 A 会话通过索引 Read 调取全文并给出正确建议
- **H3 记忆治理**：对话中指正后 Agent 自行删除错误记忆；面板删除即时生效；时间线可审计
- **三期真实验收中发现并修复的缺陷**：
  - **PROMPT_TASK 引导不足（G1）**：deepseek-v4-flash 不主动委派子代理，自行 3 Glob + 11 Grep + 11 Read 淹没主上下文（25 万+ tokens）。已强化：明确要求多关键词/多目录/多文件的全面搜索先委派子代理，并给出自包含 prompt 与结论+证据的输出要求
  - **审批超时 notify 文案用当前配置值（已修复）**：原实现 setTimeout 回调中读取 `#approvalTimeoutMs`，配置在等待期间被恢复时文案显示新值。PermissionWaiter 抽取重构后 `effectiveTimeout` 在回调外按发起时快照（`session-approval.ts`），文案与实际超时一致（2026-08 确认）
- **已知限制**：deepseek-v4-flash 在显式要求时才使用 Task 子代理，不会主动委派（产品能力已验证，属模型行为差异）

### 2026-08 批次（三期后新增，非验收用例，已推送）

- **完成可信度三层兜底**：done 软拦截（todo 有未完成项时注入提示继续，防死循环上限 2 次）+ 0 工具调用完成警告条 + 完成协议强化（完成前更新 todo、跑验证命令、禁止只输出计划）
- **任务验收链（开发验证用）**：`behavior.completionReview` 开启或 `/review` 手动触发时，任务完成后独立审查（读文件/跑验证命令核对要求），不通过自动打回修复（≤2 次）；运行时默认关闭
- **审批信息增强**：审批卡片附 Agent 本轮意图（purpose）；风险翻译扩充（去 cd 前缀 + 脚手架 / git init / commit / npx 规则）；挂起审批 ≥2 时「全部允许（本次会话）」批量放行
- **环境探查自动放行**：node/pnpm/npm 版本、view、which/uname/env 等搭建场景高频命令自动放行（`&&` 链逐段判定不豁免写段）
- **交付摘要**：会话完成且有写操作时展示改动文件与验证结果（纯事件流推导零成本）
- **BrowserCheck 工具**：无头浏览器页面检查（HTTP / title / console 错误 / 渲染后文本），模型可验证 UI 真实渲染
- **插件动态工具注册**：插件热加载后主会话与子代理下一轮即对模型可见
- **Bash 只读白名单防写绕过**：`cat x > out` 带写重定向段不因只读原语白名单放行（`segmentWritesFile` 引号感知）
- **项目信任两段式**：`/trust` 标记目录（全局配置，设置页管理）；trust 档 + 未标记目录启动提示一次；显式声明不改变权限档位语义
- **CI 双 job**：test（typecheck+build+全量单测）+ e2e（chromium，非模型场景 14 用例；模型用例本地手工回归）；修复了 pnpm/action-setup 版本冲突等 3 个既有 CI 故障
- **回放功能移除**：会话详情保留来源筛选（全部/推理/工具/子代理/系统），删除回放条/自动播放/游标

每节用例的详细通过标准见下。

## A. 基础闭环

### TC-A1 修复一个真实 bug（核心价值验证）
- **前置**：任意含测试的 Python/JS 项目，存在一个会失败的测试
- **操作**：项目目录下 `myagent`，输入"修一下这个报错的测试：npm test 里 login 那个用例"
- **通过标准**：
  1. Agent 自主定位相关源文件（过程中有 Read/Grep 调用）
  2. 使用 Edit 精确修改，界面展示 diff
  3. 自主运行测试验证；若仍失败，自行迭代直到通过
  4. 宣布完成时给出改动摘要
  5. 全程无人工介入超过 2 次（批准除外）

### TC-A2 多步骤任务与 todo 可视化
- **操作**：输入"给这个项目加上 ESLint 配置，修掉所有 error，并把 npm run lint 加进 CI 配置"
- **通过标准**：任务开始后 todo 面板出现 ≥3 项清单；同一时刻仅一项 in_progress；随执行实时翻转为 completed；最终 lint 通过、CI 文件被正确修改

### TC-A3 编辑安全性
- **操作**：让 Agent 修改一个它尚未 Read 的文件（可诱导："直接把 config.yaml 的 port 改成 8080"）
- **通过标准**：Edit 被拒绝并提示需先 Read；Agent 自行 Read 后再改成功
- **操作**：让 Agent 替换一段在文件中出现 3 次的字符串
- **通过标准**：报"匹配多处"错误；Agent 给出更长上下文使匹配唯一后成功

### TC-A4 /init 项目初始化
- **操作**：在陌生项目中启动，不做任何操作直接问一个简单问题
- **通过标准**：Agent 不自动扫描项目（无大范围 Read/Grep），直接回答问题
- **操作**：输入 `/init`
- **通过标准**：扫描由子代理执行（主上下文无探索全文）；产出 AGENTS.md 草稿（技术栈/常用命令/目录导览/约定）；确认前不落盘；编辑后保存的版本与下次会话注入内容一致
- **操作**：从未 /init 的项目中直接派一个编码任务
- **通过标准**：任务可正常完成（现用现探索）

## B. 上下文与长会话

### TC-B1 软遗忘不丢关键信息
- **操作**：连续让 Agent 读 10 个大文件，之后再问"第三个文件里那个函数叫什么"
- **通过标准**：老工具结果已占位符化（token 占用显著下降）；Agent 通过重新 Read 恢复信息后正确回答

### TC-B2 硬压缩后可继续
- **操作**：长会话接近上下文上限（或用低阈值配置模拟）触发自动压缩，随后继续下达依赖前文背景的新指令
- **通过标准**：界面出现压缩提示与比例；压缩后 Agent 仍记得任务目标、已完成的改动与 todo 状态，新指令执行正确

### TC-B3 会话恢复（/switch）
- **操作**：一次任务做到一半直接 `Ctrl+C` 杀掉进程；重新 `myagent` 后 `/switch` 选择该会话
- **通过标准**：会话完整重放（含 todo 状态）；输入"继续"后 Agent 从断点接着干，不重复已完成的步骤

### TC-B4 打断
- **操作**：Agent 执行中输入一条新指令（不打断），观察其在本轮结束后响应；再次执行中按 Esc
- **通过标准**：软打断消息在当轮结束后被处理；Esc 立即取消模型与当前工具；Edit / MultiEdit / Write 中止后文件要么保持旧版本、要么是完整新版本，不存在半文件；Bash 整个进程组被终止，界面提示其已发生的副作用无法自动撤销

## C. 权限

### TC-C1 三档行为
- **通过标准**：strict 档下 Edit / MultiEdit / Write 与所有 Bash 均弹批准且忽略 allow 记忆；normal 档下 Edit / MultiEdit、`ls`/`npm test` 自动放行，Write 与 `git push` 弹批准；trust 档下 deny 被直接拒绝、显式 ask 仍弹批准，其余自动放行

### TC-C2 批准记忆四级
- **操作**：对 `git commit *` 依次验证四个选项
- **通过标准**："仅一次"下次仍问；"本次会话"重启后仍问、会话内不再问；"本项目"写入项目 config 且新会话不再问但其他项目仍问；"全局"任意项目不再问；四类规则在 Web 设置页可见可删

### TC-C3 黑名单豁免不存在
- **操作**：trust 档下诱导 Agent 执行 `rm -rf` 类命令
- **通过标准**：直接拒绝且不出现临时强制放行入口；工具错误回灌 Agent 后必须换方案

### TC-C4 无人值守 fail-closed
- **操作**：normal 档起一个任务，出现审批请求后不响应
- **通过标准**：超时（默认 5 分钟）自动拒绝，Agent 换路继续或优雅收尾，不僵死

### TC-C5 审批内容（看完再批）
- **操作**：strict 档下让 Agent 修改一个文件，观察审批卡片
- **通过标准**：批准前展示真实 diff（±3 行上下文），diff 与最终落盘完全一致；大改动折叠为统计可展开
- **操作**：触发一次 `npm install xxx` 审批
- **通过标准**：卡片含命令原文、风险翻译（"将修改 package.json 与 lock 文件"）、Agent 的目的；未识别命令显示"副作用未知"

### TC-C6 拒绝并留言
- **操作**：审批时选"拒绝并留言"，填"别用 npm，用 pnpm"
- **通过标准**：工具未执行；留言回灌后 Agent 下一条命令改用 pnpm

## D. 无人值守

### TC-D1 时间盒 + 优雅终止
- **操作**：交互界面中 `/run 持续优化测试覆盖率 --goal "pnpm test 全过" --bounds "不改 src/api/" --until <当前时间+40分钟> --permission trust`
- **通过标准**：
  1. 三个阶段依次出现（收窄范围 → 进入收尾 → 只输出总结），事件流可见 `wrapup_warning`
  2. 收尾优先使代码处于可编译/测试通过状态；需要撤销时只能通过 EditJournal 撤销 Agent 自己且未被再次修改的编辑，禁止宽泛 `git checkout/reset`；无法安全恢复时保留现场并报告
  3. 产出三件套：总结报告（含验证结果对比）、todo 快照、记忆写入
  4. 启动前展示了 bounds 编译出的候选 deny 规则清单并经确认；清单与 bounds 语义一致；`--bounds` 中的目录全程未被修改
  5. `--permission trust` 在任务结束后回落会话原档位，后续交互不受影响

### TC-D2 预算盒熔断
- **操作**：设置极小 `--budget` 跑一次任务
- **通过标准**：花费达上限触发优雅终止；以 API 返回 usage 为对账口径，累计 token 与之一致（误差 <5%）

### TC-D3 通知
- **操作**：起一个会完成的短任务，保持 Web 页面打开
- **通过标准**：页面内状态实时变为"已完成"，标签页标题出现提示；全程无需任何外部配置
- **（后续扩展）**：配置 webhook 后跑任务，完成时收到外部推送，含任务摘要与会话链接；出错任务收到 error 级推送

## E. Web 界面

### TC-E1 监控台
- **操作**：同时跑两个会话（一个交互一个 `/run` 任务），打开 `myagent --web`
- **通过标准**：首页两张状态卡，进度/花费/时长实时刷新；数据与 CLI 端一致

### TC-E2 会话详情与回放
- **操作**：进入一个已完成会话，开启回放模式
- **通过标准**：可从第一条事件顺序复盘全部决策（文本、工具、diff、子代理卡片齐全）

### TC-E3 远程审批（手机场景）
- **前置**：配置监听内网 IP + 访问密码
- **操作**：手机浏览器打开 Web，触发一次审批请求
- **通过标准**：无密码无法访问；输入密码后看到审批卡片；点"允许"后 Agent 端立即继续（≤2 秒）

### TC-E4 配置界面自动生成
- **操作**：在 Config Schema 中新增一个配置项（不改任何前端代码），刷新设置页
- **通过标准**：新配置项自动出现在设置页，类型控件正确，保存后生效（hot 项立即生效）

### TC-E5 记忆面板
- **操作**：跑一个任务让 Agent 写入至少一条记忆，打开记忆面板
- **通过标准**：能看到该条记忆及"它学到了什么"时间线记录；手动编辑后下次会话生效

## F. 模型与成本

### TC-F1 多供应商切换
- **操作**：将 explore 角色配置为 OpenAI 兼容端点（如 DeepSeek），触发一次子代理探索
- **通过标准**：子代理正常完成；该轮 token 计入会话累计
- **操作**：在 Web 设置页为同一供应商配置两个模型，分别点击各自的“测试连接”
- **通过标准**：两次测试明确显示实际模型名、延迟与独立结果；认证失败、模型/路径错误、限流/额度不足、网络不可达有可区分提示

### TC-F2 每轮 token 透明
- **操作**：进行一次多轮对话，观察每轮回复末尾的小字
- **通过标准**：每轮显示 in/out token、缓存命中率、会话累计；数值与 API 返回 usage 一致；`/cost` 输出与逐轮累计吻合

## G. 子代理

### TC-G1 上下文隔离有效性
- **操作**：让 Agent "全面搜索这个仓库的鉴权相关代码"
- **通过标准**：主上下文中只出现结论（含 文件:行号 证据），不出现搜索过程的全文；主会话 token 增量远小于直接搜索（数量级差异）

### TC-G2 子代理失败降级
- **操作**：构造一次子代理中途失败（如超时）
- **通过标准**：主循环不中断；主 Agent 收到"部分结论 + 失败说明"并决定重试或自己完成

## H. 记忆复利

### TC-H1 跨会话记忆
- **操作**：会话 1 中告诉 Agent "这个项目用 pnpm，别用 npm"（Agent 应写入记忆）；开新会话让 Agent 安装依赖
- **通过标准**：新会话中 Agent 直接使用 pnpm，无再次询问

### TC-H2 跨项目联想
- **操作**：项目 A 的记忆中已有一条 Chroma 踩坑记录；在项目 B（也用 Chroma）遇到相似问题
- **通过标准**：Agent 通过标题索引发现项目 A 的记忆并调取全文，给出包含该经验的解决方案

### TC-H3 记忆治理
- **操作**：任务后打开记忆面板，找到一条错误记忆并删除；另在对话中指出"上次你记的 XX 是错的，删掉"
- **通过标准**：面板删除即时生效且下次会话不再注入；对话中指正后 Agent 自行修正记忆文件；每次自动写入在时间线中可审计

## I. 任务启动（/run）

### TC-I1 /run 一次性任务
- **操作**：交互界面中 `/run 清理所有 console.log`（normal 档，不加约束）
- **通过标准**：任务在当前会话持续运行直至完成，输出总结（改动统计 + 测试结果）；记忆/todo 照常写入；`/switch` 切走再切回，会话内容完整

### TC-I2 入口收敛
- **操作**：检查 shell 层可用命令
- **通过标准**：仅 `myagent` 与 `myagent --web` 两个入口；无 `-p`/`run`/`resume` 等 shell 子命令；任务、恢复全部通过内部命令完成
